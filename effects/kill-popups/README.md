# Kill pop-ups

Press **Run** when a box covers the page. It removes fixed or sticky elements that cover at least
30% of the screen above the page (z-index 10 or more), then lifts the scroll lock such boxes put on
the page. Small floating buttons are kept, and so is anything holding text or images the MangaX
scan has picked up. It says how many it removed.
